# Test Tutorial
### @explicitHints true

## {Step 1}

**Say hello**

Drag a `||game:splash||` block into the `||loops(noclick):on start||` block.

#### ~ tutorialhint
```blocks
game.splash("Hello!")
```

```blockconfig
scene.setBackgroundColor(1)
```
